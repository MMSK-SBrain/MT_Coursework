---
phase: 01-instrument-a-pre-post-self-assessment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - courses/inclusive-management-shop-floor/generate_participant_form.py
  - courses/inclusive-management-shop-floor/assessments/pre-post-self-assessment-participant.docx
autonomous: true

must_haves:
  truths:
    - "Participant form prints correctly on A4 in black and white with 21 items readable at 11pt minimum"
    - "Pre (AM) and Post (PM) columns are visually distinct — Post column has light grey shading"
    - "All 7 domain groups are separated by visual breaks with NO section labels visible to participant"
    - "Every item is first-person, present-tense, behavioral — no attitudes or intentions"
    - "Health Sensitivity domain items (16-18) use health-general language only"
    - "No indication of reverse-scoring appears anywhere on the participant form"
    - "Form title is 'My Management Practices' — words 'assessment', 'evaluation', 'test' do not appear"
    - "Self-reflection section with 3 questions appears below the items"
    - "Optional domain-averaging grid appears at the bottom"
    - "Anonymous tear-off strip appears at the very bottom separated by dashed line"
  artifacts:
    - path: "courses/inclusive-management-shop-floor/generate_participant_form.py"
      provides: "Python script that generates the participant .docx"
      min_lines: 150
    - path: "courses/inclusive-management-shop-floor/assessments/pre-post-self-assessment-participant.docx"
      provides: "Generated participant form Word document"
  key_links:
    - from: "generate_participant_form.py"
      to: "assessments/pre-post-self-assessment-participant.docx"
      via: "python-docx doc.save()"
      pattern: "doc\\.save.*pre-post-self-assessment-participant\\.docx"
---

<objective>
Generate the participant self-assessment form as a Word (.docx) document using python-docx.

Purpose: This is the core instrument participants interact with twice on training day (Pre at 9 AM, Post at 4:45 PM). It must be print-ready, psychometrically sound, and culturally calibrated. The form is deliberately positioned as a personal reflection tool, not an assessment.

Output: A Python generator script and the resulting .docx file containing all 21 behavioral items in a 3-column table with Pre/Post columns, self-reflection section, domain-averaging grid, and anonymous tear-off strip.
</objective>

<execution_context>
@/Users/mmsk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mmsk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Volumes/Dev/Course_Generator/.planning/PROJECT.md
@/Volumes/Dev/Course_Generator/.planning/ROADMAP.md
@/Volumes/Dev/Course_Generator/.planning/STATE.md
@/Volumes/Dev/Course_Generator/.planning/phases/01-instrument-a-pre-post-self-assessment/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the participant form generator script</name>
  <files>
    /Volumes/Dev/Course_Generator/courses/inclusive-management-shop-floor/generate_participant_form.py
  </files>
  <action>
First, install python-docx:
```
pip install python-docx
```

Then create `generate_participant_form.py` in `/Volumes/Dev/Course_Generator/courses/inclusive-management-shop-floor/`. This script generates the participant self-assessment form .docx. Read ALL content from `01-RESEARCH.md` — the complete 21-item set, exact wording, form structure, design specs, and code examples are there.

The script must produce the following structure in this exact order:

**1. Page Setup**
- A4 paper (21cm x 29.7cm)
- 1.5cm margins all sides
- Default font: Calibri

**2. Header Block**
- Title: "My Management Practices" — bold, 16pt, centered
- Subtitle: "A personal reflection tool -- for your eyes only" — italic, 11pt, centered
- Privacy statement: "This form is not collected by the facilitator. It is yours to keep." — bold, 10pt, centered
- Blank line, then: "Name (optional): ________________    Date: ________________" — 10pt, left-aligned

**3. Scale Legend**
- Single line: "1 = Almost Never  |  2 = Rarely  |  3 = Sometimes  |  4 = Often  |  5 = Almost Always" — 9pt, centered, with a small space above and below

**4. Item Table (3-column table)**
- Column widths: 11.7cm (item text), 3.15cm (Pre AM), 3.15cm (Post PM)
- Table style: 'Table Grid'

Header row:
- Cell 0: "Statement" — bold, 11pt
- Cell 1: "Pre (AM)" with line break then "1   2   3   4   5" — bold header text 10pt, scale numbers 10pt, centered
- Cell 2: "Post (PM)" with line break then "1   2   3   4   5" — bold header text 10pt, scale numbers 10pt, centered
- Apply grey shading (#DCDCDC) to cell 2 (Post column header)

Item rows — all 21 items from RESEARCH.md, in exact order:
- Domain 1 (Foundation): Items 1, 2, 3
- Separator row after item 3
- Domain 2 (Daily Interactions): Items 4, 5, 6
- Separator row after item 6
- Domain 3 (Self-Awareness): Items 7, 8, 9
- Separator row after item 9
- Domain 4 (Empathy): Items 10, 11, 12
- Separator row after item 12
- Domain 5 (Feedback & Conflict): Items 13, 14, 15
- Separator row after item 15
- Domain 6 (Health Sensitivity): Items 16, 17, 18
- Separator row after item 18
- Domain 7 (Team Inclusion): Items 19, 20, 21
- No separator after item 21

Each item row:
- Cell 0: "{number}. {item_text}" — 11pt Calibri
- Cell 1: "1   2   3   4   5" — 10pt, centered in cell
- Cell 2: "1   2   3   4   5" — 10pt, centered in cell, with #DCDCDC shading
- Minimum row height: 0.85cm (AT_LEAST rule)

CRITICAL: Items 5, 11, and 18 are reverse-scored but NO indication of this appears on the participant form. They are written exactly as in RESEARCH.md — the same as any other item.

Separator rows between domains:
- Height: 0.3cm (EXACT rule)
- All cells merged into one
- Background: #F0F0F0 (very light grey)
- No text

**5. Self-Reflection Section** (below the table)
- A horizontal line (can be done via a paragraph border or repeated dashes)
- "SELF-REFLECTION -- Complete this after filling in the Post (PM) column" — bold, 11pt
- "Look at both columns. Where your rating changed, reflect on what shifted." — 10pt, italic
- Three numbered questions with blank lines for writing (use underscore characters for lines):
  1. "Which group of items showed your biggest change from morning to afternoon?"
  2. "Which single item surprised you the most -- either this morning or now?"
  3. "When you return to your team, what ONE behavior will you focus on first?"
- Each question followed by a blank line: "___________________________________________________________________"

**6. Domain-Averaging Grid** (below self-reflection)
- Prefaced with: "OPTIONAL: Calculate your averages (add the three ratings, divide by 3)" — bold, 9pt
- 5-column table: Group | Items | Pre avg | Post avg | Change
- 7 data rows (Group 1 through Group 7), showing item numbers for each group
- Final row: "Total | All items | __ /105 | __ /105 |"
- 9pt font throughout this table
- Keep this compact to minimize page overflow

**7. Anonymous Tear-Off Strip** (very bottom)
- Dashed line with scissors: a paragraph containing the text scissors emoji or "---" dashes to simulate tear-off
- "OPTIONAL: Anonymous Data Strip -- Tear off and drop in the box at the front." — bold, 9pt
- "Do not write your name." — 9pt
- Three lines for data:
  - "My Pre (AM) total score (add all 21 Pre ratings):   _______  / 105"
  - "My Post (PM) total score (add all 21 Post ratings): _______  / 105"
  - "My biggest single-item shift (how many points?):    _______  points"
- All at 9pt font

**8. Save the document**
- Use `__file__`-based path resolution to ensure the script saves to the correct location regardless of working directory. Example: `import os; SCRIPT_DIR = os.path.dirname(os.path.abspath(__file__)); ASSESSMENTS_DIR = os.path.join(SCRIPT_DIR, 'assessments'); os.makedirs(ASSESSMENTS_DIR, exist_ok=True); doc.save(os.path.join(ASSESSMENTS_DIR, 'pre-post-self-assessment-participant.docx'))`
- Save to: `assessments/pre-post-self-assessment-participant.docx`
- The script must print a confirmation for each major section as it completes: 'PASS: Header block', 'PASS: Item table (21 items)', 'PASS: Separator rows (6)', 'PASS: Self-reflection', 'PASS: Domain-averaging grid', 'PASS: Tear-off strip', 'PASS: File saved'. Wrap the main execution in a try/except that prints 'FAIL: [error message]' and exits with sys.exit(1) on any exception.

Implementation notes:
- Use the `apply_shading` function pattern from RESEARCH.md for cell shading (OxmlElement approach)
- Use `WD_ROW_HEIGHT_RULE.AT_LEAST` for item rows, `WD_ROW_HEIGHT_RULE.EXACT` for separator rows
- For the header row, set bold formatting on the paragraph runs
- Ensure all paragraphs in Pre/Post columns use center alignment (WD_ALIGN_PARAGRAPH.CENTER)
- The item text column paragraphs should be left-aligned (default)
- Do NOT use the word "assessment," "evaluation," or "test" anywhere in the document
- Do NOT include domain names/labels anywhere on the form
- Print a confirmation message when the file is saved
  </action>
  <verify>
1. Run the script: `cd /Volumes/Dev/Course_Generator/courses/inclusive-management-shop-floor && python3 generate_participant_form.py`
2. Confirm the script exits without errors
3. Confirm the file exists: `ls -la assessments/pre-post-self-assessment-participant.docx`
4. Run a quick validation: `python3 -c "from docx import Document; doc = Document('assessments/pre-post-self-assessment-participant.docx'); tables = doc.tables; print(f'Tables: {len(tables)}'); print(f'Rows in main table: {len(tables[0].rows)}'); print(f'First item text starts with: {tables[0].rows[1].cells[0].text[:30]}...')"` — should show the main table with 21 item rows + 6 separator rows + 1 header row = 28 rows minimum
  </verify>
  <done>
- generate_participant_form.py exists and runs without errors
- pre-post-self-assessment-participant.docx is generated in the assessments/ directory
- Document contains a 3-column table with all 21 items in correct order
- Post (PM) column cells have grey shading (#DCDCDC)
- Both Pre (AM) and Post (PM) columns appear on the same table row — Pre responses remain visible simultaneously during Post completion (no fold-line, reveal, or separate page design)
- Domain separator rows (6 total) exist between each group of 3 items
- No domain labels appear on the form
- Items 5, 11, 18 appear identical to all other items (no reverse-score marking)
- Self-reflection section with 3 questions is present below the table
- Domain-averaging grid is present
- Anonymous tear-off strip is present at the bottom
- Title reads "My Management Practices" — no "assessment/evaluation/test" language
- Privacy statement and optional Name field are in the header

Requirements covered: FORM-01, FORM-02, FORM-03, FORM-04, FORM-05, FORM-06, FORM-07, FORM-08, FORM-09, FORM-10, FORM-11, FORM-12, FORM-13, FORM-14, FORM-15, PROD-01, PROD-02, PROD-03
  </done>
</task>

</tasks>

<verification>
1. Script runs cleanly: `python3 generate_participant_form.py` produces no errors
2. Output file exists at the correct path: `assessments/pre-post-self-assessment-participant.docx`
3. Programmatic verification: Load the .docx and confirm table structure (row count, column count, shading presence)
4. Spot-check item content: Verify item 1 text matches RESEARCH.md, verify item 21 text matches, verify item 5 (reverse-scored) has no marking
5. Spot-check structure: Verify header says "My Management Practices", verify privacy statement is present, verify self-reflection questions are present
</verification>

<success_criteria>
- The .docx file opens correctly and displays a professionally formatted participant form
- All 21 items appear in the correct domain order with correct text from RESEARCH.md
- The form is print-ready for A4 black-and-white printing
- No psychometric information (domain labels, reverse-score markers) is visible on the form
- The generator script is deterministic and can be re-run to regenerate the document
</success_criteria>

<output>
After completion, create `.planning/phases/01-instrument-a-pre-post-self-assessment/01-01-SUMMARY.md`
</output>
